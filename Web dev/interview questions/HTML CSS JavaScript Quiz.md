
# Quiz 1

1. What is the default position of an HTML element?
    - `static`
    - `relative`
    - `absolute`
    - `fixed`

Answer:
```
static
```

---

2. How do you make text bold using CSS?
    - `font-style: bold;`
    - `font-weight: bold;`
    - `text-bold: true;`
    - `bold: yes;`

Answer:
```css
font-weight: bold;
```

---

3. How do you apply an external CSS file to an HTML document?
    - `<style src="styles.css">`
    - `<link rel="stylesheet" href="styles.css">`
    - `<css src="styles.css">`
    - `<stylesheet> styles.css </stylesheet>`

Answer:
```html
<link rel="stylesheet" href="styles.css">
```

---

4. Which property is used to change the text color in CSS?
    - `font-color`
    - `text-color`
    - `color`
    - `foreground-color`

Answer:
```css
color
```

---

5. Which HTML tag is used to define a hyperlink?
    - `<link>`
    - `<href>`
    - `<hyper>`
    - `<a>`

Answer:
```html
<a>
```

---

6. What is the default position of an HTML element?
    - `static`
    - `relative`
    - `absolute`
    - `fixed`

Answer:
```
static
```

---

7. What is the correct syntax for adding an image in HTML?
    - `<img src="image.jpg">`
    - `<image src="image.jpg">`
    - `<img href="image.jpg">`
    - `<img link="image.jpg">`

Answer:
```html
<img src="image.jpg">
```

---

8. Which tag is used to define a form in HTML?
    - `<input>`
    - `<form>`
    - `<fieldset>`
    - `<button>`

Answer:
```html
<form>
```

---

9. Which HTML element is used to define an unordered list?
    - `<ul>`
    - `<list>`

Answer:
```html
<ul>
```

---

10. Which HTML tag is used to define internal CSS?
    - `<script>`
    - `<style>`
    - `<css>`
    - `<link>`

Answer:
```html
<style>
```

---

11. Which CSS property is used to change the background color?
    - `color`
    - `background-color`
    - `bgcolor`
    - `background`

Answer:
```css
background-color
```

---

12. Which tag is used to create a drop-down list in a form?
    - `<list>`
    - `<select>`
    - `<dropdown>`
    - `<option>`

Answer:
```html
<select>
```

---

13. What is the default file extension for an HTML file?
    - `.htm`
    - `.html`
    - `.html5`
    - `Both a and b`

Answer:
```
.html
```

---

14. Which HTML tag is used to define a footer for a document or section?
    - `<bottom>`
    - `<footer>`
    - `<section>`
    - `<end>`

Answer:
```html
<footer>
```

---

15. Which CSS property controls the spacing between elements?
    - `padding`
    - `margin`
    - `spacing`
    - `border-spacing`

Answer:
```css
margin
```

---

16. Which of the following is the correct way to define a custom data attribute in HTML5?
    - `<div attribute="value">`
    - `<div data-custom="value">`
    - `<div custom-data="value">`
    - `<div data="custom:value">`

Answer:
```html
<div data-custom="value">
```

---

17. How do you specify a background color in HTML?
    - `<body background="red">`
    - `<body bgcolor="red">`
    - `<background color="red">`
    - `<bgcolor="red">`

Answer:
```html
<body bgcolor="red">
```

---

18. Which CSS unit is relative to the size of the parent element?
    - `px`
    - `em`
    - `vh`
    - `cm`

Answer:
```css
em
```

---

19. Which HTML tag is used to create an input field for text?
    - `<textfield>`
    - `<input type="text">`
    - `<input type="textfield">`
    - `<text>`

Answer:
```html
<input type="text">
```

---

20. In HTML5, which of the following tags is used to define self-contained content, such as a blog post or news article?
    - `<section>`
    - `<div>`
    - `<article>`
    - `<aside>`

Answer:
```html
<article>
```

---

21. What does CSS stand for?
    - `Computer Style Sheets`
    - `Creative Style Sheets`
    - `Cascading Style Sheets`
    - `Colorful Style Sheets`

Answer:
```
Cascading Style Sheets
```

---

22. How do you make a flex container in CSS?
    - `display: flex;`
    - `flexbox: enable;`
    - `layout: flex;`
    - `flex-container: true;`

Answer:
```css
display: flex;
```

---

23. Which tag is used to define a table in HTML?
    - `<table>`
    - `<tab>`
    - `<tbl>`
    - `<t>`

Answer:
```html
<table>
```

---

24. Which CSS property is used to change the font of text?
    - `font-style`
    - `font-family`
    - `font-type`
    - `text-font`

Answer:
```css
font-family
```

---

25. Which HTML element is used to provide metadata about an HTML document, such as character encoding and viewport settings?
    - `<head>`
    - `<meta>`
    - `<script>`
    - `<link>`

Answer:
```html
<meta>
```

---

# Quiz 2 - JavaScript

1. Which of the following statements about setTimeout is true?
    - `setTimeout blocks the execution of the next statement`
    - `setTimeout is executed in the main thread synchronously`
    - `setTimeout executes the callback function asynchronously after the specified time`
    - `setTimeout guarantees precise execution timing`

Answer:
```javascript
setTimeout executes the callback function asynchronously after the specified time
```

---

2. What will the following code log to the console? `(function() { var a = b = 5; })(); console.log(typeof a); console.log(typeof b);`
    - `undefined, undefined`
    - `undefined, number`
    - `number, number`
    - `ReferenceError, ReferenceError`

Answer:
```javascript
undefined, number
```

---

3. What will be the output of `console.log(typeof [])` in JavaScript?
    - `array`
    - `object`
    - `undefined`
    - `list`

Answer:
```javascript
object
```

---

4. Which of the following is a JavaScript framework?
    - `React`
    - `Angular`
    - `Vue`
    - `All of the above`

Answer:
```
All of the above
```

---

5. What does `innerHTML` represent?
    - `The element’s outer structure`
    - `The CSS style of the element`
    - `The HTML content inside an element`
    - `The JavaScript object`

Answer:
```
The HTML content inside an element
```

---

6. What will be the output of the following code? `console.log(typeof null);`
    - `null`
    - `undefined`
    - `object`
    - `string`

Answer:
```javascript
object
```

---

7. What will be the output of this code? `let a = { x: 1 }; let b = a;x = 2;console.log(a.x);`
    - `1`
    - `2`
    - `undefined`
    - `null`

Answer:
```javascript
1
```

---

8. Which of the following is the correct way to declare a variable in JavaScript?
    - `var myVar;`
    - `let myVar;`
    - `const myVar;`
    - `All of the above`

Answer:
```javascript
All of the above
```

---

9. What is the difference between `let`, `var`, and `const` in JavaScript?
    - `let and const are block-scoped, while var is function-scoped`
    - `const variables cannot be reassigned, while let and var can`
    - `let and const were introduced in ES6, while var is older`
    - `All of the above`

Answer:
```javascript
All of the above
```

---

10. What will be the output of `console.log(2 + "2");`?
    - `4`
    - `22`
    - `NaN`
    - `Error`

Answer:
```javascript
22
```

---

11. What will `console.log(0.1 + 0.2 === 0.3);` return?
    - `true`
    - `false`
    - `undefined`
    - `NaN`

Answer:
```javascript
false
```

---

12. How do you remove an element from the DOM?
    - `element.destroy()`
    - `parent.removeChild(element)`
    - `element.removeNode()`
    - `delete element`

Answer:
```javascript
parent.removeChild(element)
```

---

13. How do you write "Hello, World!" in an alert box?
    - `alert("Hello, World!");`
    - `msg("Hello, World!");`
    - `msgBox("Hello, World!");`
    - `print("Hello, World!");`

Answer:
```javascript
alert("Hello, World!");
```

---

14. What does the `map()` function do in JavaScript?
    - `Iterates over an array and modifies its elements`
    - `Creates a new array by applying a function to each element`
    - `Filters an array based on a condition`
    - `Sorts an array in ascending order`

Answer:
```javascript
Creates a new array by applying a function to each element
```

---

15. Which of the following is true about the `async` function in JavaScript?
    - `It automatically retries failed promises.`
    - `It returns a non-blocking callback.`
    - `It always returns a Promise.`
    - `It executes synchronously by default.`

Answer:
```javascript
It always returns a Promise.
```

---

16. How can you change the content of an HTML element with `id="demo"`?
    - `document.getElement('demo').innerText = "Hello"`
    - `document.querySelector('#demo').text = "Hello"`
    - `document.getElementById('demo').innerHTML = "Hello"`
    - `document.getId('demo').innerContent = "Hello"`

Answer:
```javascript
document.getElementById('demo').innerHTML = "Hello"
```

---

17. How do you check if a variable is an array in JavaScript?
    - `typeof variable === "array"`
    - `variable instanceof Array`
    - `Array.isArray(variable)`
    - `Both b and c`

Answer:
```javascript
Both b and c
```

---

18. What does `document.createElement('p')` do?
    - `Creates a paragraph and adds it to the DOM`
    - `Creates a new paragraph element`
    - `Selects the first <p> tag`
    - `Modifies an existing paragraph`

Answer:
```javascript
Creates a new paragraph element
```

---

19. What does `===` operator do in JavaScript?
    - `Compares only the values`
    - `Compares both value and type`
    - `Assigns values`
    - `Checks for null values`

Answer:
```javascript
Compares both value and type
```

---

20. What does the following JavaScript statement return? `console.log(typeof function() {});`
    - `function`
    - `object`
    - `undefined`
    - `callable`

Answer:
```javascript
function
```

---

21. What will be the output of this JavaScript snippet? `console.log(2 ** 3);`
    - `8`
    - `6`
    - `5`
    - `None of the above`

Answer:
```javascript
8
```

---

22. What is the correct syntax for referring to an external JavaScript file?
    - `<script src="script.js">`
    - `<script href="script.js">`
    - `<script ref="script.js">`
    - `<script name="script.js">`

Answer:
```html
<script src="script.js">
```

---

23. How do you define a function in JavaScript?
    - `function myFunction() { }`
    - `def myFunction() { }`
    - `void myFunction() { }`
    - `define myFunction() { }`

Answer:
```javascript
function myFunction() { }
```

---

24. What will be the output of the following code? `const promise = new Promise((resolve, reject) => { resolve("Success"); reject("Error"); }); promise.then(result => console.log(result));`
    - `Error`
    - `"Success"`
    - `"Error"`
    - `Undefined`

Answer:
```javascript
"Success"
```

---

25. Which of the following is the root of the DOM tree?
    - `window`
    - `document.body`
    - `document`
    - `html`

Answer:
```javascript
document
```

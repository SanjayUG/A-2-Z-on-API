export function add(x, y) {
    return x + y;
}

export function sub(x, y) {
    return x - y;
}

export function product(x, y) {
    return x * y;
}

export function divide(x, y) {
    return x / y;
}

// let result = add(20, 20);
// console.log(result);


// if only one function then use:
// export default add;
// 📌 Objects & JSON
// JSON is the main data format used in APIs

let user = {
  id: 1,
  name: "Krishna",
  role: "API Engineer"
};

console.log("User Name:", user.name);
console.log("As JSON:", JSON.stringify(user));
